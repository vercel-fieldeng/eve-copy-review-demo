// eve resolves the `workflow` and `workflow/api` specifiers used inside
// workflow tools to its vendored Workflow SDK. This reference gives the
// project the matching ambient types without installing the SDK separately.
/// <reference types="eve/workflow-modules" />
